"""
    python -m dis dis_4.py
    
    Python在編譯的時候會附帶一些優化
    可以用dis來觀察哪些程式碼會被優化
"""

# 寫出來就已經確定結果的內容，會被優化的程式碼
a = 1 + 2
b = 'abc' + '123'
c = [1, 2, 3] + [4, 5, 6]
d = "-" * 10

# 因為不確定結果，所以不會被優化的程式碼
A = a + 1
B = b + 'def'
C = c + [7] + [8, 9] # 受前面影響，這邊也不會被優化